const box = /* css */ `
  border-radius: 5px;
  box-shadow: 3px 3px 6px -4px rgba(0, 0, 0, 0.45);
  color: var(--text);
  border: 1px solid var(--text);
  background-color: var(--background);
`

export { box }
